<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para Mi Coincidencia Más Linda</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            width: 100%;
            height: 100%;
            font-family: 'Georgia', serif;
            background-color: #0d0d0d;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Contenedor del fondo */
        .background-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        /* Imagen con oscurecimiento correcto y zoom suave */
        .background-image {
            width: 100%;
            height: 100%;
            background-image: url('foto.jpg'); 
            background-size: cover;
            background-position: center;
            filter: brightness(0.35) contrast(1.05);
            transform: scale(1.02);
            animation: subtleZoom 25s infinite alternate ease-in-out;
        }

        /* Capa con gradiente rosa/oscuro para amarrar los colores */
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, rgba(13, 13, 13, 0.6) 0%, rgba(255, 182, 193, 0.12) 100%);
            z-index: 2;
        }

        /* Caja contenedora premium para aislar el texto y asegurar lectura */
        .content-card {
            position: relative;
            z-index: 3;
            text-align: center;
            padding: 35px 25px;
            max-width: 500px;
            width: 88%;
            background: rgba(13, 13, 13, 0.55);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5);
            opacity: 0;
            animation: elegantFadeIn 2s ease-out forwards;
            animation-delay: 0.3s;
        }

        /* Texto estilizado con máxima legibilidad */
        .romantic-text {
            color: #ffffff;
            font-size: 1.8rem;
            line-height: 1.5;
            font-weight: 300;
            letter-spacing: 0.5px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
        }

        /* Destacado en rosa suave */
        .romantic-text span {
            color: #ffb6c1; 
            font-weight: 400;
            border-bottom: 1px dashed rgba(255, 182, 193, 0.4);
            padding-bottom: 2px;
        }

        /* Botón de Música Estilo Minimalista */
        .music-btn {
            position: absolute;
            bottom: 45px;
            z-index: 4;
            background: rgba(255, 255, 255, 0.07);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            color: #ffffff;
            padding: 12px 28px;
            border-radius: 30px;
            font-size: 0.75rem;
            font-weight: bold;
            cursor: pointer;
            letter-spacing: 2px;
            transition: all 0.3s ease;
        }

        .music-btn:hover {
            background: rgba(255, 182, 193, 0.15);
            border-color: #ffb6c1;
            transform: translateY(-1px);
        }

        /* Animaciones estables */
        @keyframes subtleZoom {
            0% { transform: scale(1.02); }
            100% { transform: scale(1.06); }
        }

        @keyframes elegantFadeIn {
            0% {
                opacity: 0;
                transform: translateY(15px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Correcciones finas para celulares pequeños */
        @media (max-width: 480px) {
            .content-card {
                padding: 25px 20px;
            }
            .romantic-text {
                font-size: 1.45rem;
                line-height: 1.4;
            }
            .music-btn {
                bottom: 35px;
                padding: 10px 22px;
            }
        }
    </style>
</head>
<body>

    <div class="background-container">
        <div class="background-image"></div>
        <div class="overlay"></div>
    </div>

    <div class="content-card">
        <p class="romantic-text">
            "A mí más linda coincidencia, feliz cumple mes de nuestro <span>primero besito</span>, Te amo"
        </p>
    </div>

    <button class="music-btn" id="musicToggle">REPRODUCIR MÚSICA</button>
    <audio id="romanticAudio" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>

    <script>
        const audio = document.getElementById('romanticAudio');
        const btn = document.getElementById('musicToggle');

        btn.addEventListener('click', () => {
            if (audio.paused) {
                audio.play().catch(e => console.log("Audio bloqueado:", e));
                btn.textContent = "PAUSAR MÚSICA";
                btn.style.background = "rgba(255, 182, 193, 0.15)";
                btn.style.borderColor = "#ffb6c1";
            } else {
                audio.pause();
                btn.textContent = "REPRODUCIR MÚSICA";
                btn.style.background = "rgba(255, 255, 255, 0.07)";
                btn.style.borderColor = "rgba(255, 255, 255, 0.15)";
            }
        });
    </script>
</body>
</html>
